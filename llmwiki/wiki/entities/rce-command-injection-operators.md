---
title: Command Injection Operators
type: entity
related: [[Command Injection]]
---

Các delimiter/operator được nhắc đến:

- Unix: `;`, `|`, `||`, `&`, `&&`, `` `cmd` ``, `$(cmd)`, `$()`, `${IFS}`, newline/tab
- Windows: `&`, `|`, `||`, `^`

Đây là các ký tự thường dùng để tách hoặc nối lệnh.